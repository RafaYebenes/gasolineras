import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'yebenesapps.app.gasoliapp',
  appName: 'gasolineras',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
